импортировать  {  useEffect ,  useState ,  useRef  }  из  «реагировать»

экспортировать  const  useMyHookTest  =  ( значение )  =>  {
    const  [ текст , setText ]  =  useState ( ) ;

    использоватьЭффект ( ( ) => {
        установитьтекст ( значение )
    } , [ значение ] )

    возвращаемый  текст ;
}

экспортировать  const  useFetchData  =  ( ссылка )  =>  {
    const  url  =  `https://jsonplaceholder.typicode.com/ ${ ссылка } ` ;
     константный кеш  =  useRef ( { } ) ;
    const  [ текст ,  setText ]  =  useState ( ) ;
    использоватьЭффект ( ( ) => {
        если  ( кеш . текущий [ URL ] )  {
            setText ( кеш.текущий [ url ] ) ; _ _
        }
        еще  {
            получить ( адрес )
                . затем ( res  =  > res .json ( ) )
                . тогда ( res  =>  {
                    установить текст ( разрешение ) ;
                    кеш . текущий [ URL ]  =  текст ;
                } )
        }
    } , [ ссылка ] )
    возвращаемый  текст ;
}